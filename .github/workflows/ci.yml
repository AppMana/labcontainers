name: ci
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with: {go-version-file: go.mod}
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - uses: actions/setup-node@v4
        with: {node-version: "22", cache: npm}
      - run: go test ./...
      - run: go vet ./...
      - run: python -m pip install -e .
      - run: python -m unittest discover -s python/tests -v
      - uses: astral-sh/setup-uv@v6
      - run: make generate-containerlab
      - run: make generate-cloud-init
      - run: git diff --exit-code -- schemas/cloud-init pkg/cloudinit/networkconfig/zz_generated.go pkg/cloudinit/networkconfig/zz_open_generated.go python/labcontainers/cloudinit_generated.py python/labcontainers/CLOUD-INIT-LICENSE
      - run: git diff --exit-code -- schemas/containerlab python/labcontainers/containerlab_generated.py python/labcontainers/CONTAINERLAB-LICENSE
      - run: python -m py_compile python/labcontainers/*.py images/*/launcher.py
      - run: npm ci
      - run: node --test javascript/*.test.js
      - run: node -e "const {Client} = require('./'); if (!Client.launch) process.exit(1)"
      - run: npm pack --dry-run
